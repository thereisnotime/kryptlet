apiVersion: v1
kind: Service
metadata:
  name: {{ include "kryptlet.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kryptlet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kryptlet.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
